# Spring Boot
/brentversal/.gradle
/brentversal/build/
/brentversal/out/
*.class
*.jar
.claude/
application-secret.properties

# React
/frentversal/node_modules/
/frentversal/build/
/frentversal/.env

## Python 가상환경 (각자 컴퓨터에서 만드는 것이라 깃에 올리지 않는다)
#/prentversal/
.venv/

/nbproject/private/
/nbbuild/
/dist/
/nbdist/
/.nb-gradle/
build/
!**/src/main/**/build/
!**/src/test/**/build/

# IDE
.idea/
*.iml
.vscode/
.DS_Store



application-local.properties
brentversal/.env

# 프로젝트 로컬 문서 (깃허브에 올리지 않음)
/SCAFFOLD_PLAN.md
/CLAUDE.md
/planning/

.env
# 화면 확인용으로 웹에서 받아 둔 동네 사진.
# 저작권이 확인되지 않아 저장소·배포에 넣지 않는다.
# 로컬에서는 C:/upload/files 로 복사해 /files 경로로 띄운다(DB image_url 은 로컬 DB 에만 넣는다).
/반포동.png
/서초동.png
.env
.env.bak
*.env.bak
.env
.env.bak
*.env.bak
