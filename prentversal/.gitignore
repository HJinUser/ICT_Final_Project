# =========================================================
# Python 가상환경
# 설치된 패키지들이 들어있는 로컬 환경이며,
# requirements.txt로 동일한 환경을 다시 만들 수 있다.
# =========================================================
.venv/


# =========================================================
# Python 캐시
# Python 실행 과정에서 자동 생성되는 임시 파일이며,
# 삭제해도 필요할 때 다시 생성된다.
# =========================================================
__pycache__/
*.pyc
*.pyo
*.pyd


# =========================================================
# 환경변수 / API Key
# 실제 API Key와 같은 비밀정보가 들어간다.
# .env 파일이 없으면 .env.example을 복사해서 .env로 만든 뒤
# 필요한 실제 API Key 값을 입력해서 사용한다.
# =========================================================
.env


# =========================================================
# pytest 캐시
# pytest 실행 과정에서 자동 생성되는 임시 데이터이며,
# 테스트를 다시 실행하면 재생성된다.
# =========================================================
.pytest_cache/


# =========================================================
# PyCharm 설정
# 사용하는 PC와 PyCharm 환경에 따라 달라지는
# 개인 개발환경 설정 파일이다.
# =========================================================
.idea/
*.iml


# =========================================================
# 로그 파일
# 프로그램 실행 과정에서 자동 생성되는 실행 기록이다.
# =========================================================
*.log


# =========================================================
# 머신러닝 원본 데이터
# API로 다시 수집하거나 공공데이터에서 다시 다운로드할 수 있는
# 대용량 원본 데이터이므로 Git에 올리지 않는다.
# =========================================================
data/raw/


# =========================================================
# WordCloud 이미지
# 텍스트마이닝 실행 시 다시 생성할 수 있는 발표/확인용 결과물이다.
# =========================================================
outputs/*.png